import {withStyles} from '@material-ui/core/styles';

export default withStyles({
	'@global': {
		// '.MuiButton-root': {
		// color: 'white',
		// },
		// '.MuiIconButton-root': {
		// color: 'white',
		// },
	},
})(() => null);
